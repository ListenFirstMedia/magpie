---
qa_id: QA-43915
title: Ads Account IDs Radaac Report
component: Radaac
labels: []
source: https://listenfirstmedia.atlassian.net/browse/QA-43915
date_fetched: 2026-06-08
---

# QA-43915 — Ads Account IDs Radaac Report

## Description (verbatim from Jira)
> This test case ensures Ads Account IDs Radaac Report

## Inferred steps (Jira spec is title + 1-line description only — no explicit Steps section)
1. Login to `radaac.lfmdev.in` (Cognito SSO via Google).
2. Locate the "Ads Account IDs" report row in the report list.
3. Click the report row → modal opens.
4. Default `file_format=tsv`. Optionally pick `csv` or `xls`.
5. Submit → page redirects to `/ads_account_ids?...` with status `Fetching report` → eventually surfaces a download link `/cache/YYYYMMDDAdsAccountIds_<hash>.<ext>`.
6. Click the download link → file saves to ~/Downloads.

## Assertions (inferred)
- A1: Modal opens with description text "Retrieves Facebook, Twitter and Tiktok Ads account IDs for Ad accounts we have authentication for."
- A2: Filter inputs include Subscriber Name, PDA, File Format, Column Headers, Cache.
- A3: Submission produces a file download with filename pattern `YYYYMMDDAdsAccountIds_<hash>.<ext>`.
- A4: CSV content is well-formed (comma-separated, headers row present when `column_headers=on`).
