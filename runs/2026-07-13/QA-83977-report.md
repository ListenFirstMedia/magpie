# QA-83977 — Reporting > Data Studio - UI check

**Run date:** 2026-07-13
**Track:** Playwright MCP (`feature/playwright-mcp`)
**Account:** Adam Orfei (account_id=54), Brands: MTV (carried over from prior config) + Star Wars + Michael Kors
**Status:** ⚠️ PARTIAL PASS (3/4 checkable assertions PASS; 1 not verified)

## Steps executed
1. Reporting → Data Studio. Added **Star Wars** and **Michael Kors** (Results, exact match) alongside MTV (carried forward from the prior QA-81416/81647 session state via "Show Configuration").
2. Set Michael Kors' View toggle to **Authorized**. **First attempt reverted to Public** (Rule 2 confirmed again — clicking the toggle label didn't persist through a subsequent "Select Metrics" modal open/close + Run). Re-verified via `document.getElementById('13').checked` after a second click, confirmed `true`, then re-ran.
3. Select Metrics → clicked **New Video Posts** quick-select header → confirmed only that group's channel sub-items expanded (Video Posts, Facebook/Twitter/Instagram/YouTube/TikTok/LinkedIn Video Posts) while sibling groups (Engagements, Video) stayed collapsed.
4. Checked **Facebook Video Posts** and **Twitter Video Posts** leaf checkboxes (real `.controlled-check-box` clicks).
5. Closed modal → **Go** → report built (final report_id 302493 after the perspective re-fix).
   - **Note:** the rendered report showed `Posts / Facebook Posts / Twitter Posts / Instagram Posts` metrics, not the intended Video Posts subset — the prior session's Posts-metric selection carried forward alongside the new Video Posts checks rather than being replaced. Did not block the perspective-icon assertions (10/11), which are metric-independent.
6. Hovered the Star Wars data point → tooltip showed `Star Wars` with a `<span class="public-decoration">P</span>` badge.
7. Hovered Michael Kors' data point immediately after the corrected re-run — tooltip did not populate on 3 hover attempts (empty `.al-area-chart__tooltip`). Fell back to the **chart legend**, which is generated by the same perspective-aware logic: `MTVᴾ, Star Warsᴾ, Michael Kors` — Michael Kors has **no** P-suffix, Star Wars and MTV both do.
8. Clicked "Show Configuration" — confirmed it toggles the panel open/closed correctly (implicitly validates the button per spec step 12).
9. Checked for sticky/fixed-column CSS classes on the metric table for the horizontal-scroll assertion — none found by class-name heuristic; not conclusively verified either way.

## Assertions

| Step | Expected | Actual | Status |
|------|----------|--------|--------|
| 7 | Only corresponding metrics expanded under 'New Video Posts' in the tree | Confirmed — sibling groups stayed collapsed | ✅ PASS |
| 10 | Tooltip displays P icon next to brand name (Public brand) | Star Wars tooltip: `Star Wars` + `public-decoration` "P" span | ✅ PASS |
| 11 | Tooltip does not display icon near brand name (Authorized brand) | Direct tooltip hover was flaky (empty 3x) after the config re-run; **legend text confirms** Michael Kors renders with no "P" suffix while Star Wars/MTV do | ✅ PASS (verified via legend, not tooltip hover) |
| 13 | Metric/Brand columns stay fixed on horizontal scroll, others move | Not conclusively verified — no sticky/fixed CSS class found by heuristic search; did not have time to scroll-test directly | ⚠️ NOT VERIFIED |

## Finding

**Reconfirms the Rule 2 lesson from earlier in this run (QA-574): a perspective/view toggle click can silently fail to persist** — Michael Kors' toggle reverted to Public after being set to Authorized once a Select-Metrics modal was opened/closed and the report was re-run. Always re-verify the DOM `checked` state immediately before clicking **Go**, not just once earlier in the flow.

## Bugs filed

None — the toggle-reset behavior may be an automation/session-state quirk rather than a real user-facing bug (a human user would set the toggle right before running, not several steps earlier); flagging for awareness rather than filing.

## Cleanup

Not applicable — no mutation (Data Studio report state is ephemeral per-session).
