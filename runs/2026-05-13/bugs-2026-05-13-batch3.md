# Bug candidates — 2026-05-13 batch 3 (QA-110071, QA-109059, QA-106221)

## 🐛 BC-1 — Custom Data Set display order on Brand > Content doesn't match creation order

- **Source case:** QA-109059
- **Severity:** P2 (failing assertion A2 of a Blocker-priority test, but does not block usage)
- **First seen:** 2026-05-13
- **Affected area:** Brand > Content → Data Set dropdown → Custom Data Set section

### What failed

Assertion A2 of QA-109059 expects: "Ensure the created custom data sets are displayed in the created order."

Actual ordering observed in the dropdown vs creation timestamps from Settings > Custom Data Sets:

| UI order | Custom Data Set | Created | Out of order? |
|---------:|-----------------|---------|:-------------:|
| 1 | Test 3 Dupes | May 9, 2025 | — |
| 2 | performance test | May 23, 2025 | — |
| 3 | performance test 2 | Jun 5, 2025 | — |
| 4 | Test | Jul 16, 2025 | — |
| 5 | Main Test 1 | Mar 28, 2025 | **yes — should be first** |
| 6 | Some new data set name | Apr 18, 2025 | **yes — should be second** |

### Expected vs Actual

```diff
Assertion:  table (cross-source: dropdown order vs created_at order)
Expected:   ["Main Test 1", "Some new data set name", "Test 3 Dupes",
             "performance test", "performance test 2", "Test"]   # asc by created_at
Actual:     ["Test 3 Dupes", "performance test", "performance test 2",
             "Test", "Main Test 1", "Some new data set name"]
Mismatched: positions 1, 2, 3, 4, 5, 6  (every position)
```

### Reproduction

1. Log in as `lfiqa@listenfirstmedia.com`, switch to Adam Orfei account.
2. Navigate to Brand > Content for MTV (`brand_id=4018`).
3. Click the Data Set dropdown.
4. Scroll to the "Custom Data Set" section.
5. Compare order against Settings > Custom Data Sets (where the Created Date column is visible).

### Suggested next steps

- Backend: check the sort key used by the API that returns the custom-data-set list for the brand>content dropdown. If it's `last_used` or similar, the test case wording needs to be updated to match. If it should be `created_at` ASC, fix the sort.
- QA: confirm with the original test case author (Prasanna J) whether the intended sort is "creation date" or "last used / last edited."

---

## ⚠ BC-2 — Brand > Audience: "Export → Metrics" produces no observable file

- **Source case:** QA-110071
- **Severity:** P3 (Minor priority case, but if there's truly no download it's a real bug)
- **First seen:** 2026-05-13
- **Affected area:** Brand > Audience → Export dropdown → Metrics

### What was observed

Clicking `Export → Metrics` (with Threads channel selected, Mar 16 – Mar 22, 2025 date range) fires Mixpanel telemetry but produces no file detectable by:
- `URL.createObjectURL` interception
- `HTMLAnchorElement.prototype.click` with `download` attribute interception
- `window.open` interception
- `HTMLFormElement.prototype.submit` interception
- New tab in MCP tab group

### Status

**INCONCLUSIVE.** Two possibilities:
1. The export does produce a file in the user's Downloads folder but via a path my hooks miss (e.g., a fetch + showSaveFilePicker, or a server response with Content-Disposition: attachment that the browser handles silently). Manual Downloads-folder check would resolve.
2. The export silently fails — the click is registered for telemetry but nothing else happens.

### Suggested next steps

- User: check Downloads folder for a recent `Brand-Audience-Metrics.*` file. If present, BC-2 is resolved as "not a bug, automation friction only." If absent, file as a real bug.

---

## ⛔ BC-3 — QA-106221 blocked on missing QA-106218 starting state (not a bug, process issue)

QA-106221 expects a Custom Data Set created in QA-106218 with a specific initial 7-metric order ending in Impressions. None of Adam Orfei's 6 existing custom data sets matches.

**Not a product bug — a test-execution sequencing issue.** Either run QA-106218 first or rewrite QA-106221 to create its own starting state.
